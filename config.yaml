server:
  host: "localhost"
  port: 8080
  timeout: 30

database:
  host: "localhost"
  port: 5432
  name: "myapp"
  user: "postgres"
  password: "password"

logging:
  level: "info"
  file: "app.log"
